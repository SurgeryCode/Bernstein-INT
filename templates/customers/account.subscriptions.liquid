<div class="container">
    <div class="account-hld">
        <div class="account-left">
            {% render 'account-main-box' %}
            {% render 'account-nav', active_item: 'subscriptions' %}
        </div>
        <div class="account-right">
            <h1 class="account-title">{{ 'customer.account.subscriptions' | t }}</h1>
            <div class="account-content">
                {% section 'account-subscriptions' %}
            </div>
        </div>
    </div>
</div>