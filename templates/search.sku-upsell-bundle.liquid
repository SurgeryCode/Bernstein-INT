{%- layout none -%}

{%- if search.results_count > 0 -%}
    {%- for product in search.results -%}
        {%- render 'upsell-item', product: product, action: 'add-to-bundle' -%}
    {%- endfor -%}
{%- endif -%}